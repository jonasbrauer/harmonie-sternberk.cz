version: '3'
services:
  nginx:
    build:
      context: docker
      dockerfile: nginx/Dockerfile
    ports:
      - "8080:80"
    networks:
      - internal
    volumes:
      - ./dist:/var/www/html/
  
  php:
    build:
      context: docker
      dockerfile: php/Dockerfile
    networks: 
      - internal
    volumes:
      - ./dist:/var/www/html
      - ./docker/php/data:/var/www/data

  db:
    image: mysql
    restart: always
    ports:
      - 3306:3306
    networks: 
      - internal
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: harmonie

  adminer:
    image: adminer
    restart: always
    networks: 
      - internal
    ports:
      - 8081:8080

networks:
  internal:
    driver: bridge
